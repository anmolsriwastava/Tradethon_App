## 🔒 Security

This project is **private and proprietary**. API keys and secrets are never committed to GitHub.

- All sensitive data is stored in `.env` (ignored by git)
- Use `.env.example` as a template
- Branch protection is enabled on `main`
- Only authorized collaborators have write access
- 2FA required for all contributors